# RQ5' (docs/RQ_SUMMARY.md §6.5) — the INVERSE task: program + return value -> a call that
# returns it. MODEL-NEUTRAL. NO H1 (task='input' grids refuse it in eval_vllm.run_grid).
#
# WHY THIS EXISTS. Every adapter in the project was trained on output prediction only. If
# what tuning teaches is a fact about the CODE (reading it under a transform), it should be
# usable in either direction; if it is a fact about the forward MAPPING, the inverse task
# should show base-level accuracy — Nikiema et al. 2025's "cognitive specialization", which
# the CFT replication (docs/CFT_REPLICATION.md) found at 0 % on deobfuscation. This is the
# stress test for "understanding": same items, same code, the question asked backwards.
#
# Design.
# * ONE prompt for every arm, with the frozen L0 one-shot demo (prompts.ONE_SHOT_DEMOS,
#   shown in its inverse form). No arm was trained on this prompt, so the demo is the same
#   distribution shift for all of them; without it the tuned arms — trained to answer with a
#   bare literal — would fail on format alone, and the read would be about the format, not
#   the task. `formatonly` is here to measure exactly that residue.
# * Grading is by EXECUTION (src/obtune/inverse.py): any call that reproduces the value is
#   correct, as in CRUXEval-I. `args_exact` (the gold call) is a secondary column.
# * max_tokens 128: a call with list/dict arguments is longer than a return literal.
#   `stop` keeps the base config's "\n\n" / fence; the call is one line.
# * Two cell blocks are submitted as two stages (core / specialists) so neither needs an
#   8-hour backfill slot; both write to this phase.
_extends: _base_eval.yaml

phase: inverse_generic
experiment_id: inverse_stacks
run_tag: inverse_stacks
language: python
eval_source: heldout
task: input

engine:
  gpu_memory_utilization: 0.85

sampling:
  max_tokens: 128
  # "\n" ADDED 2026-09-14. The backward answer is ONE call on ONE line by contract, but every
  # backward config inherited the forward stop list ("\n\n", fences, <end_of_turn>) and never
  # stopped at a single newline. A model that writes the call and then opens a fresh
  # "Language: python / Program:" block produced a multi-line reply that the grader rejects by
  # design -- 22,893 replies on the panel, 21,140 of them in CodeGemma's TIES merge, where they
  # were 65 % of ALL its trials and the whole reason that arm read as unreadable. Safe: 0 of
  # 68,310 sampled replies begin with a newline, so this cannot truncate to empty. Cells already
  # written are re-graded on their first line by 72_regrade_inverse.py, which is the identical
  # measurement (see that script). max_tokens stays at 128: gold calls have p99 = 64 tokens.
  stop: ["\n", "\n\n", "```", "<end_of_turn>"]

scoring:
  float_tol: 1.0e-6
  exec: {timeout_s: 2.0, mem_mb: 512, workers: 16}

systems:
  - {name: base, arch: none, one_shot: true}
# `formatonly` is DELIBERATELY ABSENT. That control adapter was only ever trained for
# CodeLlama-7B, and a named adapter that is not on disk is a hard error (2026-09-11), so its
# presence failed this config on the other seven models six minutes in. 7B's formatonly
# stack cells were written by the first run and are unaffected.
  - {name: tuned_L0, adapter: "runs/adapters/{model}/{language}/L0_r32_s17/best", arch: per_type, train_cond: L0, one_shot: true}
  - {name: mono_all, adapter: "runs/adapters/{model}/{language}/L0-L1b-L1r-L2-S1-S2_r32_s17/best", arch: per_type, train_cond: mix, one_shot: true}
  - {name: cons_lam3, adapter: "runs/adapters_objectives/{model}/{language}/L0-L1b-L1r-L2-S1-S2_r32_cons_parent_lam3_s17/best", arch: per_type, train_cond: mix, one_shot: true}
  - {name: tuned_X1, adapter: "runs/adapters/{model}/{language}/X1_r32_s17/best", arch: per_type, train_cond: X1, one_shot: true}
  - {name: merge_dare_ties, adapter: "runs/adapters/{model}/{language}/merge_dare_ties_r32_s17", arch: per_type, train_cond: merge, one_shot: true}

# THE BACKWARD TASK ON STACKED OBFUSCATION (2026-09-13). inverse_generic covers the seven ladder
# conditions only, so the master table's input-prediction block stops where the paper's argument
# starts -- there is no backward number on any stack, seen or unseen-containing.
#
# Same phase as inverse_generic: cells key on (phase, system, condition), so nothing already written
# is recomputed and the stack rows simply fill in beside the ladder rows.
eval_conditions: [C_L1b_S1, C_L1r_S1, C_S1_L1r, C_L2_S4, C_L1r_S3, C_S4_S3,
                  C3_L1r_S3_S4, C3_S1_S3_S4, C3_L1r_S1_S4, C4_L1r_S1_S3_S4,
                  C_L1r_X1, C_X1_S1, C_S2_X1, C3_L1r_S1_X1, C_L1r_X1m, C_S1_X1s]
