# H-F2-route — ROUTING on stacks with an UNSEEN family inside, CodeLlama-7b (2026-09-13). Derived from mole_composite_codellama-7b.yaml; see the tail.
#
# WHY THIS IS THE EXPERIMENT RQ1 IS MISSING. The claim "inference-time routing fails on stacks" has
# never been measured on this panel: routing was read on SINGLE transforms only, where the router is
# saturated (100 % route accuracy, gate entropy ~1e-6) and beats a RANDOM gate by exactly +0.0000.
# A stacked input genuinely contains two mechanisms, so a hard router must pick one and be wrong
# while a soft mixture can blend. That is the condition the hypothesis predicts and it does not
# exist anywhere in the current corpus.
#
# THE CONTROLS ARE THE POINT, as on the ladder: `mole_uniform` mixes the experts with a fixed
# uniform gate and `mole_random` with a random one, so "the mixture is worth something" and "the
# ROUTING is worth something" are separable. `mole_hardrouter` is the argmax variant.
#
# GRID A EXPLICITLY (`eval_source: heldout`). `_base_eval.yaml` leaves it unset and the default is
# the 145-176 program testset, which is how the published mole_* numbers ended up on a different
# program set from every RQ1/RQ2 headline. Cell paths key on (phase, system, condition) and not on
# grid, so that mistake is invisible until someone compares sample sizes.
#
# Ten composites: the six depth-2 SEEN stacks and the four depth-3/4 ones.
_extends: _base_eval.yaml

phase: mole_generic
experiment_id: rq1_mole_f2
run_tag: mole_f2_codellama-7b
model: codellama-7b
language: python
eval_source: heldout

experts:
  L0:   runs/adapters/codellama-7b/python/L0_r32_s17/best
  L1b:  runs/adapters/codellama-7b/python/L1b_r32_s17/best
  L1r:  runs/adapters/codellama-7b/python/L1r_r32_s17/best
  L2:   runs/adapters/codellama-7b/python/L2_r32_s17/best
  S1:   runs/adapters/codellama-7b/python/S1_r32_s17/best
  S2:   runs/adapters/codellama-7b/python/S2_r32_s17/best
  S3:   runs/adapters/codellama-7b/python/S3_r32_s17/best
  S4:   runs/adapters/codellama-7b/python/S4_r32_s17/best

gate:
  d_router: 64
  shared_query: false
gate_checkpoint: runs/mole/codellama-7b/python/routerlora_codellama7b_s17/gate.pt

engine:
  seed: 17
  dtype: bfloat16
  # 16, not the seen-stack config's 32: the X1 composites are the long prompts (medians 515-1130
  # tokens, C_S2_X1 up to ~1800) and batch 32 ran a 47 GB MIG slice out of memory 17 min in
  # (job 392884, 2026-09-13). Submitted with --exclude g-06-01 as well.
  batch_size: 16

systems:
  - {name: base, arch: none}
  - {name: mole_uniform, arch: mole_uniform, train_cond: mix}
  - {name: mole_random, arch: mole_random, train_cond: mix}
  - {name: mole_router, arch: mole_router, train_cond: mix}
  - {name: mole_hardrouter, arch: mole_hardrouter, train_cond: mix}

arm_modes: {}
# H-F2-route (2026-09-13): the same four routing arms on the six X1-containing composites, so
# routing's verdict on stacks with an UNSEEN family inside is measured rather than inherited from
# breadth. Same phase as the seen-stack routing cells; the F2 controls (mono_all, tuned_L0,
# cons_lam3) already exist in f2_divergence on the same items. Rule pre-registered in
# CLAUDE_SCRATCHPAD.md before submission; 0 mole_* cells existed on any X1 composite.
eval_conditions: [C_L1r_X1, C_X1_S1, C_S2_X1, C_L1r_X1m, C_S1_X1s, C3_L1r_S1_X1]
