# FLIP, JavaScript — item M of the ATTRIB plan (E3 replication).
#
# JavaScript answers "is the effect Python-specific?". It does NOT answer "is the effect
# Java-specific?", which is the literal form of the reviewer objection — this box has a JRE
# but no javac, so a Java replication is not reachable before the Aug 28 freeze. The
# limitations section must say the source paper's Java setting remains untested here.
#
# Differs from flip_qwen1.5b_py.yaml in `language` and nothing else, so any Python-vs-JS gap
# is attributable to the corpus rather than to the recipe.
_extends: flip_qwen1.5b_py.yaml
language: javascript
