# Invariance arm trained on L1b (python, 1.5B). Twin of grid_qwen1.5b_py_L1b.yaml;
# identical in every knob except the added L_align term.
_extends: _base_align.yaml

model: qwen25c-1.5b
language: python
train_conditions: [L1b]
run_tag: align_py_L1b
phase: main
