"""External baselines — published models run on obtune's conditions.

Each baseline reproduces its own native prompt and answer-parsing contract rather than
being forced into obtune's bare-literal format, so the comparison measures the model
rather than its fluency in our template.
"""
