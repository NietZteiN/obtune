### Target Date: 2026-08-30 (7B RQ1 transfer matrix: TR = 0.885, the diagonal barely exists)

- **Hypotheses / what we're testing:** **H-mem — LoRA tuning on output prediction over
  obfuscated code teaches transform MEMORIZATION rather than semantic invariance, at 7B.**
  CONFIRM if the transfer matrix shows a strong diagonal (each adapter helps mainly on its
  own condition) and a flat off-diagonal. REFUTE if off-diagonal gains approach diagonal
  gains, i.e. `TR` near 1. This is the 7B replication of the 1.5B RQ1 ladder.

- **Setup:** `configs/eval/grid_rq1_7b.yaml` (new), job **359114**, `h200` `g-07-08`,
  COMPLETED 00:11:45, 78 cells. 13 systems (`base` + 6 conditions x seeds 17/42) x 6 eval
  conditions, Grid A (`eval_source: heldout`), `phase: grid_rq1_7b`. Adapters from
  `configs/train/grid_qwen7b_py_*.yaml` trained 08-28 (~5 GPU-h total); checkpoint-selected
  today, jobs 359067 + 359069-359079, on the held-in `S2`/per-condition val slice.
  Analysis: cluster bootstrap by `snippet_id`, B=2000, seed 17.

- **Results:**
  - **Common subset** (all-conditions-succeeded, `../../CLAUDE.md` §4 coverage rule):
    **412 programs** of 557. Per-condition program counts: L0 557, L1b 553, L1r 557, L2 557,
    **S1 416**, S2 556 — S1 is the binding constraint, by design.
  - **Common-subset transfer matrix, seed-averaged, gain over `base`:**
    diagonal **+0.1696**, off-diagonal **+0.1496**.
  - **Cluster bootstrap by program, B=2000, n=412:**

    | statistic | point | 95% CI |
    |---|---|---|
    | diagonal gain | +0.1698 | [+0.1473, +0.1948] |
    | off-diagonal gain | +0.1499 | [+0.1281, +0.1733] |
    | diagonal − off-diagonal | **+0.0199** | [+0.0147, +0.0252] |
    | mean off-diagonal **TR** | **+0.8849** | [+0.8550, +0.9163] |

    0/2000 draws have (diagonal − off-diagonal) <= 0.
  - **`base` format_fail is 0.179** and drops to ~0.010 under every adapter.

- **What worked / hypothesis verdict:** **H-mem REFUTED at 7B.** An adapter delivers ~89 %
  of its own-condition benefit on transforms it never trained on. The specialization
  advantage is real and reliably non-zero (+2.0 points, CI excludes 0) but is an order of
  magnitude smaller than the transfer itself (+15 points). Memorization predicts a flat
  off-diagonal; the off-diagonal is nearly as high as the diagonal.

- **Observations:**
  - **The `L0` column is the strongest for almost every adapter, including the structural
    ones** — `tuned_S2_s17` reads .585 on L0 against .583 on its own S2. Together with the
    format_fail collapse (0.179 -> 0.010), the most parsimonious reading is that most of the
    gain is task/output-format competence, not transform inversion. That would make `TR`
    near 1 partly an artifact of a shared floor effect, and it is a threat to the strong
    reading of this result. A `formatonly` control arm exists
    (`configs/train/formatonly_qwen1.5b_py.yaml`) and was built for exactly this.
  - **`tuned_L1b` is the one adapter showing specialization, and it inverts:** best in the
    L1b column (.513/.512) while worst in S1 (.477/.475). It is also the adapter whose s42
    checkpoint selection chose the EARLIEST checkpoint (checkpoint-73 of 219).
  - **Checkpoint selection is not cosmetic here.** Selections ranged from `checkpoint-73`
    to `final`: L1b_s42 -> 73, L2_s42 -> 74, S1_s17 -> 108, S1_s42 -> 162, S2_s17 -> 148.
    Several conditions peak well before the end of training.
  - **The run was 11:45 against a 12:00:00 request.** `../../CLAUDE.md` §1 quotes 3-6 min
    per 1.5k-item cell at 7-8B; the true figure on H200 is ~9 s/cell (78 cells, ~125k
    generations, 11:45 total). Another A6000-era estimate that no longer holds.
  - **A grid collision was caught by the guard, not by me** (job 359113, FAILED in 4 s):
    the config was first written with `phase: main`, and `results/cells/main/qwen25c-7b/`
    already holds Grid B (`testset`, n=176) cells under the same system names. Resume would
    have pooled two grids `../../CLAUDE.md` forbids pooling, corrupting the existing Grid B
    cells rather than merely producing a bad new run. `phase` is the cell-path namespace;
    a new grid needs a new phase, registered in `TrialRow.phase`.

- **New questions / new hypotheses:** **H-format — most of the 7B gain is output-format
  competence, not invariance.** Predicts a `formatonly` adapter recovers most of the +15
  points on every condition. If it does, `TR = 0.885` is measuring a shared floor and the
  invariance claim needs a different denominator. This is the first thing to run.

- **Next Steps:** (1) Run the `formatonly` control at 7B. (2) Fit the GLMM stack
  (item-level binomial, crossed random effects for program x model, BH-FDR across the matrix
  as one family) — the bootstrap here is the coverage-honest summary, not the inferential
  model `../../CLAUDE.md` §4 asks for. (3) H1 remains unspent for 7B; it is worth spending
  only once H-format is settled.
