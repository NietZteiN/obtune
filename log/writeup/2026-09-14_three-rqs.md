# 2026-09-14 — Three RQs: brittleness, the loss term, the stress test

**Thread:** writeup · **Status:** done, rebuilt · **Related:** [`2026-09-13_shortened-to-the-page-limit.md`](2026-09-13_shortened-to-the-page-limit.md)

User: *"I think we can get rid of rq2, rq1 can show the results on other adaptations and then argue
they are brittle in one. Then rq2 is about our introducing a loss term. Then rq3 is stress testing the
loss term."*

| was | is |
|---|---|
| RQ1 do methods generalise or memorise | **RQ1 do current adaptation methods generalise** — the baselines *and* the brittleness argument |
| RQ2 what is memorised | folded into RQ1 |
| RQ3 anchoring to clean-code behaviour | **RQ2** (`\label{sec:loss}`) |
| RQ4 does the anchored model generalise | **RQ3 does the anchored model survive the same tests** (`\label{sec:stress}`) |

## What was kept from the old RQ2 rather than deleted

Its three instruments are what turn RQ1 from "these methods score worse out of distribution" into
"they are brittle *because* they key on surface", and RQ3's ladder analysis depends on one of them.
They are compressed into a single closing paragraph of RQ1, with the identifier table moving with them:

- the gain tracks identifier surface, $+3.39$ on all-seen stacks and $+5.85$ out of sample on a rule
  fixed before that stimulus existed;
- the unseen family's two mechanisms do not transfer to each other yet either alone recovers ~90 % of
  the whole adapter's gain;
- breadth becomes a more committed guesser rather than a worse reader.

Cut: the sub-headed structure, the "what to anchor to" section (now two sentences), and the extended
statement of what the weakest-link test does not establish — the undecided verdict survives in Threats.

## References repointed

`sec:rq2` had two live references. RQ2's "Section~\ref{sec:rq2} ended with a requirement" becomes
`sec:rq1`; RQ3's "split by the identifier rule of Section~\ref{sec:rq2}" becomes RQ1's; Threats'
weakest-link sentence now points at RQ1. `sec:rq3`/`sec:rq4` are renamed `sec:loss`/`sec:stress` so a
future renumber cannot silently mis-point them. Build is clean: 36 pages, no errors, no undefined
references beyond the user's own `ase26` citation, no overfull boxes.

Originals in `paper/router_merger/sections_pre_3rq_2026-09-14/`.
